# Variables injected into tmux when direnv is unavailable.
# Add only non-sensitive names you are comfortable exposing to every agent session.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_BASE_URL=
PATH=
