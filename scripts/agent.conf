# Optional overrides for agent.sh; sourced before commands run.
# Example settings:
# AGENT_MAC_ATTACH=1          # Auto-open Terminal on macOS (1=yes, 0=no)
# AGENT_ENV_MODE=auto         # auto|direnv|whitelist
# AGENT_ENV_WHITELIST="OPENAI_API_KEY ANTHROPIC_API_KEY PATH"
# AGENT_PIPE_LOG=0            # 1 to tee tmux pane output into .codex.log per task
